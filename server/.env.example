# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# Local development
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/redeemer_recovery?schema=public"

# Docker (use this when running with docker-compose)
# DATABASE_URL="postgresql://postgres:postgres@postgres:5432/redeemer_recovery?schema=public"
