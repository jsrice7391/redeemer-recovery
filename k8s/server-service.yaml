apiVersion: v1
kind: Service
metadata:
  name: server-service
  namespace: redeemer-recovery
  labels:
    app: server
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: server
