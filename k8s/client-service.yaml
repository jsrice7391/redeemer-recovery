apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: redeemer-recovery
  labels:
    app: client
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: client
